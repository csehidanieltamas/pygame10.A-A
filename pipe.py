import pygame

class Pipe:
    pipe_surface: pygame.image.load() = pygame.image.load('imageSource/pipe.jpg')
    pipe_surface: pygame.transform.scale2x() = pygame.transform.scale2x(pipe_surface)
    
